import express, { request, response } from 'express';

//importar os controllers aqui dentro.

const routes = express.Router();

//const itensController = new ItemsController(); - Instanciar o controller por ser uma classe.


//routes.get('/rota', 'instacia.nomedometodod(index|create|show|delete))';
//ex = routes.post('/points', pointsController.create);


export default routes;